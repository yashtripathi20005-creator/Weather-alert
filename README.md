# Weather Alert Notifier

A Python application that monitors weather conditions and sends alerts via console, email, or Pushbullet when severe weather thresholds are exceeded.

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Configure
cp .env.example .env
# Edit .env with your API keys and settings

# Run
python main.py
